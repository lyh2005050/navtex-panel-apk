name: Build APK
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Wait for files to sync
        run: sleep 10
      - name: Setup Android
        run: |
          sudo apt update
          sudo apt install -y openjdk-17-jdk wget unzip gradle
      - name: Build APK
        run: |
          gradle assembleDebug
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: navtex-panel-apk
          path: **/*.apk
